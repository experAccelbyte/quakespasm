// Copyright (c) 2024 AccelByte Inc. All Rights Reserved.
// This is licensed software from AccelByte Inc, for limitations
// and restrictions contact your company contract manager.

// This code is generated by tool. DO NOT EDIT.
#pragma once

#include "accelbyte/session/session_export.h"
#include "accelbyte/session/game_session/AdminGetGameSessions.h"
#include "accelbyte/session/game_session/572a30a45a8c7c9onsByAttributes.h"
#include "accelbyte/session/game_session/AdminDeleteGameSessions.h"
#include "accelbyte/session/game_session/AdminSetDSReady.h"
#include "accelbyte/session/game_session/AdminKickGameSessionMember.h"
#include "accelbyte/session/game_session/AdminUpdateGameSessionMember.h"
#include "accelbyte/session/game_session/CreateGameSession.h"
#include "accelbyte/session/game_session/GetGameSessions.h"
#include "accelbyte/session/game_session/JoinSessionWithCode.h"
#include "accelbyte/session/game_session/GetGameSessionByPodName.h"
#include "accelbyte/session/game_session/DeleteGameSession.h"
#include "accelbyte/session/game_session/GetGameSession.h"
#include "accelbyte/session/game_session/PatchGameSession.h"
#include "accelbyte/session/game_session/UpdateGameSession.h"
#include "accelbyte/session/game_session/1e85a5f3923aeadnBackfillTicket.h"
#include "accelbyte/session/game_session/RevokeGameSessionCode.h"
#include "accelbyte/session/game_session/GenerateGameSessionCode.h"
#include "accelbyte/session/game_session/InviteToGameSession.h"
#include "accelbyte/session/game_session/JoinGameSession.h"
#include "accelbyte/session/game_session/PromoteGameSessionLeader.h"
#include "accelbyte/session/game_session/LeaveGameSession.h"
#include "accelbyte/session/game_session/KickGameSessionMember.h"
#include "accelbyte/session/game_session/RejectGameSessionInvitation.h"
#include "accelbyte/session/game_session/GetSessionServerSecret.h"
#include "accelbyte/session/game_session/AppendMemberToGameSession.h"
#include "accelbyte/session/game_session/CancelGameSession.h"
#include "accelbyte/session/game_session/GetMyGameSessions.h"
#include "accelbyte/session/models/AppendMemberGameSessionRequest.h"
#include "accelbyte/session/models/CreateConfigAlertRequest.h"
#include "accelbyte/session/models/ConfigAlert.h"
#include "accelbyte/session/models/ConfigTemplate.h"
#include "accelbyte/session/models/ConfigTemplates.h"
#include "accelbyte/session/models/CreateConfigTemplateRequest.h"
#include "accelbyte/session/models/CreateGameSessionRequest.h"
#include "accelbyte/session/models/CreatePartyRequest.h"
#include "accelbyte/session/models/DSInfo.h"
#include "accelbyte/session/models/DeleteGameSessionsRequest.h"
#include "accelbyte/session/models/DeleteGameSessions.h"
#include "accelbyte/session/models/DeletePartySessions.h"
#include "accelbyte/session/models/DeletePartySessionsResponse.h"
#include "accelbyte/session/models/EnvVariables.h"
#include "accelbyte/session/models/EnvVariable.h"
#include "accelbyte/session/models/GameSessions.h"
#include "accelbyte/session/models/GameSession.h"
#include "accelbyte/session/models/GlobalConfig.h"
#include "accelbyte/session/models/JoinByCodeRequest.h"
#include "accelbyte/session/models/PartyKick.h"
#include "accelbyte/session/models/SessionPagination.h"
#include "accelbyte/session/models/PSNAppServerCredentialRecord.h"
#include "accelbyte/session/models/Pagination.h"
#include "accelbyte/session/models/Parties.h"
#include "accelbyte/session/models/PartySession.h"
#include "accelbyte/session/models/PlayerAttributesRequestBody.h"
#include "accelbyte/session/models/PlayerAttributes.h"
#include "accelbyte/session/models/PlayerPlatform.h"
#include "accelbyte/session/models/PlayersCurrentPlatformRequest.h"
#include "accelbyte/session/models/PlayersCurrentPlatform.h"
#include "accelbyte/session/models/PromoteLeaderRequest.h"
#include "accelbyte/session/models/ConfigInfo.h"
#include "accelbyte/session/models/UpdateGlobalConfigRequest.h"
#include "accelbyte/session/models/65f791541e2ae44dentialsRequest.h"
#include "accelbyte/session/models/MemberRequest.h"
#include "accelbyte/session/models/DeleteGameSession.h"
#include "accelbyte/session/models/DeletedPartySessions.h"
#include "accelbyte/session/models/ServerSecret.h"
#include "accelbyte/session/models/SessionInviteRequest.h"
#include "accelbyte/session/models/SessionInvite.h"
#include "accelbyte/session/models/SessionStorageRequest.h"
#include "accelbyte/session/models/SetDSReadyRequest.h"
#include "accelbyte/session/models/UpdateConfigTemplateRequest.h"
#include "accelbyte/session/models/94f65b9df7cb67cBackfillRequest.h"
#include "accelbyte/session/models/UpdateGameSessionMemberStatus.h"
#include "accelbyte/session/models/UpdateGameSessionRequest.h"
#include "accelbyte/session/models/UpdatePartyRequest.h"
#include "accelbyte/session/models/User.h"
#include "accelbyte/session/models/XblCertificate.h"
#include "accelbyte/session/models/LogConfig.h"
#include "accelbyte/session/models/Session.h"
#include "accelbyte/session/models/SessionMember.h"
#include "accelbyte/session/models/DSMConfigRecord.h"
#include "accelbyte/session/models/DefaultDSMCConfig.h"
#include "accelbyte/session/models/ExtendConfig.h"
#include "accelbyte/session/models/GameServer.h"
#include "accelbyte/session/models/MemberActiveSession.h"
#include "accelbyte/session/models/SessionSetting.h"
#include "accelbyte/session/models/PSNAppServerCredentials.h"
#include "accelbyte/session/models/PartyMembers.h"
#include "accelbyte/session/models/PlatformCredentials.h"
#include "accelbyte/session/models/PortConfigAMS.h"
#include "accelbyte/session/models/RecentPlayerQuery.h"
#include "accelbyte/session/models/c1e21684fe5f509eSessionRequest.h"
#include "accelbyte/session/models/Team.h"
#include "accelbyte/session/models/UserInfo.h"
#include "accelbyte/session/models/UserPlatform.h"
#include "accelbyte/session/models/XBLCertificateCredential.h"
#include "accelbyte/session/models/Error.h"
#include "accelbyte/common/Error.h"
#include "accelbyte/tls/SecurityAuthorization.h"

#include <functional>
#include "accelbyte/memory/memory.h"

namespace accelbyte {
namespace session {

class GameSession
{
public:	
    /**
    * @brief Get all game sessions.
    * @details Get all game sessions.
    * @details /session/v1/admin/namespaces/{namespace}/gamesessions
    *
    */
    AB_session_EXPORT static void admin_get_game_sessions(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminGetGameSessions& request, 
        const std::function<void(const model::GameSessions&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Query game sessions by admin
    * @details Query game sessions by admin.

By default, API will return a list of available game sessions (joinability: open).
Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

query parameter \&#34;availability\&#34; to filter sessions&#39; availability:
all: return all sessions regardless it&#39;s full
full: only return active sessions
default behavior (unset or else): return only available sessions (not full)

    * @details /session/v1/admin/namespaces/{namespace}/gamesessions
    *
    */
    AB_session_EXPORT static void admin_get_game_sessions_by_attributes(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminGetGameSessionsByAttributes& request, 
        const std::function<void(const model::GameSessions&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Delete bulk game sessions.
    * @details Delete bulk game sessions.
    * @details /session/v1/admin/namespaces/{namespace}/gamesessions/bulk
    *
    */
    AB_session_EXPORT static void admin_delete_game_sessions(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminDeleteGameSessions& request, 
        const std::function<void(const model::DeleteGameSessions&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Admin set the DS ready to accept connection
    * @details When the session template has ds_manual_set_ready as true. Then the DS need to calls this end point in order to notify game client if the DS is ready to accept any game client connection.

    * @details /session/v1/admin/namespaces/{namespace}/gamesessions/{sessionId}/ds
    *
    */
    AB_session_EXPORT static void admin_set_ds_ready(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminSetDSReady& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Kick member from a game session.
    * @details Kick member from a game session.
    * @details /session/v1/admin/namespaces/{namespace}/gamesessions/{sessionId}/members/{memberId}/kick
    *
    */
    AB_session_EXPORT static void admin_kick_game_session_member(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminKickGameSessionMember& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Update status of a game session member.
    * @details Update a game session member status.
    * @details /session/v1/admin/namespaces/{namespace}/gamesessions/{sessionId}/members/{memberId}/status/{statusType}
    *
    */
    AB_session_EXPORT static void admin_update_game_session_member(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AdminUpdateGameSessionMember& request, 
        const std::function<void(const model::UpdateGameSessionMemberStatus&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Create a game session.
    * @details Create a game session.
Session configuration &#34;name&#34; is mandatory, this API will refer following values from the session template if they&#39;re not provided in the request:
- type
- joinability
- autoJoin. If enabled (set to true), players provided in the request will automatically joined the initial game session creation. Game session will not send any invite and players dont need to act upon it.
- minPlayers
- maxPlayers
- inviteTimeout
- inactiveTimeout
- attributes
- dsSource
- tieTeamsSessionLifetime
- matchPool
- clientVersion
- deployment
- serverName
- textChat
- autoJoin
- requestedRegions
- dsSource
- preferredClaimKeys
- fallbackClaimKeys
- customURLGRPC

When the tieTeamsSessionLifetime is true, the lifetime of any partyId inside teams attribute will be tied to the game session.
Only applies when the teams partyId is a game session.

Session has 2 fields for user status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
User statuses:
1. INVITED: by default, to join a session (except session with OPEN joinability or if session configuration has &#34;autoJoin&#34; sets to True) a user will receive an invite. The invitee will have the chance to respond within &#34;inviteTimeout&#34; which you can configure through session configuration.
2. TIMEOUT: when a user is invited to a session, they will receive an invite. Unless &#34;disableResendInvite&#34; sets to True in the session configuration, the user will also receive invite reminder every 30s until they respond to the invite. 
3. REJECTED: when a user rejects an invite. To rejoin an INVITE_ONLY session, they will need to be re-invited and accept the invite.   
4. JOINED: there are few ways of a user to join a session, by invite, direct join (depends on session joinability) or join by code. upon invite, once a user accepts an invite, their status will be changed to JOINED. 
5. LEFT: user can leave a session. in case of party, a user can only be in 1 party at a time. therefore when they decide to create or join another party, they will be automatically removed from their initial party and their status will be changed to LEFT.
6. KICKED: only party leader can kick a member.   
7. DISCONNECTED: if user still have reserved seat in the session and they disconnect lobby websocket, their status in the session will be changed to DISCONNECTED and field &#34;members.previousStatus&#34; of that user will contains the initial status before they disconnect lobby websocket. the user will be given chance to reconnect within &#34;inactiveTimeout&#34; which you can configure through session configuration.
8. CONNECTED: when a user reconnect their lobby websocket, their status will change from DISCONNECTED to CONNECTED, only if they previously JOINED session. if they were on INVITED state before the disconnect happened, the user&#39;s status will return back to INVITED after they reconnect.
9. DROPPED: when &#34;inactiveTimeout&#34; is due and user never re-establish their websocket connection, we will drop them from the session.
10. TERMINATED: only applies to game session. If a game session (match) is ended, DS will end/delete the session and we will mark all remaining users&#39; status to be TERMINATED.  
11. CANCELLED: when a session joinability changes to CLOSED, any remaining invites will be canceled.  

User is considered as active if they&#39;re present in the session, which their status either CONNECTED or JOINED.
User has a reserved seat in the session if their status either INVITED, JOINED, CONNECTED, DISCONNECTED. When user&#39;s&#39; status change to other than these mentioned statuses, we will release the seat for other players to occupy.

Managing the relation between session and lobby websocket connection:
- Session relies on lobby to consider player&#39;s connection health to our backend. therefore a disruption to lobby websocket will be reflected in the user&#39;s status in all of their session(s). 
- If user still have a reserved seat in the session and they disconnect lobby websocket, their status in session(s) will be changed to DISCONNECTED and field &#34;members.previousStatus&#34; of that user will contains the initial status before they disconnect lobby websocket. This &#34;members.previousStatus&#34; used to track user&#39;s previous status before they disconnect websocket, since we still reserve a seat for them, therefore this field will be empty again after they websocket.
- If the disconnected user is the leader of the session they&#39;re disconnected from, we will wait until &#34;leaderElectionGracePeriod&#34; is due, to promote the next oldest member as the new leader of the session. You can configure &#34;leaderElectionGracePeriod&#34; through session configuration.
- The user will be given chance to reconnect within &#34;inactiveTimeout&#34; which you can configure through session configuration. If until &#34;inactiveTimeout&#34; is due and the user doesn&#39;t reconnect their websocket, they will be removed from session and their status will change to DROPPED. If the dropped user was the leader of the session, we will promote the next oldest member as leader.
- By default, we will update user&#39;s status to what it was before disconnect, when the user reconnects lobby websocket, unless &#34;manualRejoin&#34; sets to True in the session configuration. When &#34;manualRejoin&#34; is enabled, after lobby websocket reconnect, the game client will need to manually invoke join session again to rejoin the session. 
- If the user was on INVITED state before the disconnect happened, the user&#39;s status will return back to INVITED after they reconnect.

When the session type is a DS, a DS creation request will be sent if number of active players reaches session&#39;s minPlayers.
Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

By default, DS requests are sent to DSMC, but if dsSource is set to &#34;AMS&#34;:
- A DS will be requested from AMS instead of DSMC.
- The server will be chosen based on a set of claim keys, in order of preference, to match with fleets.
- The claim key list is built build from the preferredClaimKeys, fallbackClaimKeys, and clientVersion as follows:
	[preferredClaimKeys.., clientVersion, fallbackClaimKeys...]
for session override can follow guideline in here https://docs.accelbyte.io/gaming-services/services/extend/override-ags-feature/getting-started-with-session-customization/

    * @details /session/v1/public/namespaces/{namespace}/gamesession
    *
    */
    AB_session_EXPORT static void create_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::CreateGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_201,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Query game sessions
    * @details Query game sessions.

By default, API will return a list of available game sessions (joinability: open).
Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

query parameter \&#34;availability\&#34; to filter sessions&#39; availability:
all: return all sessions regardless it&#39;s full
full: only return active sessions
default behavior (unset or else): return only available sessions (not full)

    * @details /session/v1/public/namespaces/{namespace}/gamesessions
    *
    */
    AB_session_EXPORT static void get_game_sessions(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GetGameSessions& request, 
        const std::function<void(const model::GameSessions&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Join a session by code.
    * @details Join a session by code. The user can join a session as long as the code is valid
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/join/code
    *
    */
    AB_session_EXPORT static void join_session_with_code(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::JoinSessionWithCode& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Get game session detail.
    * @details Get game session detail by podname.
Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

    * @details /session/v1/public/namespaces/{namespace}/gamesessions/servers/{podName}
    *
    */
    AB_session_EXPORT static void get_game_session_by_pod_name(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GetGameSessionByPodName& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Delete a game session.
    * @details Delete a game session.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}
    *
    */
    AB_session_EXPORT static void delete_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::DeleteGameSession& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Get game session detail.
    * @details Get game session detail.
Session will only be accessible from active players in the session, and client with the permission, except the joinability is set to OPEN.
Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}
    *
    */
    AB_session_EXPORT static void get_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GetGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Patch update a game session.
    * @details Update specified fields from game session data.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}
    *
    */
    AB_session_EXPORT static void patch_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::PatchGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Update a game session.
    * @details Updates a game session, this endpoint will override stored gamesession data.
To update only specified fields, please use following endpoint:
method : PATCH
API	 : /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}

To update DS attributes (clientVersion, deployment, requestedRegions) it will only be applied only as long as no DS has been requested, otherwise ignored.

    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}
    *
    */
    AB_session_EXPORT static void update_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::UpdateGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Update game session backfill ticket id .
    * @details Update game session backfill ticket id. Will override game session backfill ticket based on given request parameter
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/backfill
    *
    */
    AB_session_EXPORT static void update_game_session_backfill_ticket(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::UpdateGameSessionBackfillTicket& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Revoke game session code.
    * @details Revoke code of the game session. Only leader can revoke a code.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/code
    *
    */
    AB_session_EXPORT static void revoke_game_session_code(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::RevokeGameSessionCode& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Generate a game session code.
    * @details Generate a new code for the game session. Only leader can generate a code.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/code
    *
    */
    AB_session_EXPORT static void generate_game_session_code(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GenerateGameSessionCode& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Invite a user to a game session.
    * @details Invite a user to a game session.
platformID represents the native platform of the invitee. API will return the corresponding native platform&#39;s userID.
supported platforms:
	- STEAM
	- XBOX
	- PSN
Metadata is optional parameter which will be sent over via invitation notification and is not permanently stored in the game session storage.

    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/invite
    *
    */
    AB_session_EXPORT static void invite_to_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::InviteToGameSession& request, 
        const std::function<void()>& on_201,
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Join a game session.
    * @details Join a game session.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/join
    *
    */
    AB_session_EXPORT static void join_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::JoinGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Promote new game session leader.
    * @details Promote game session member to become the new game session leader.

This API requires the NAMESPACE:{namespace}:SESSION:GAME [UPDATE] permission.

This API can be operated by:
- User (game session member) who is the current leader of the game session
- Game Client
- Dedicated Server (DS)

This API will promote game session leader candidate with the following criteria:
- Leader candidate is a member of the game session
- Leader candidate has a &#34;CONNECTED&#34; or &#34;JOINED&#34; status
- If the leader candidate is the current leader, then no promotion process is carried out

    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/leader
    *
    */
    AB_session_EXPORT static void promote_game_session_leader(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::PromoteGameSessionLeader& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Leave a game session.
    * @details Leave a game session.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/leave
    *
    */
    AB_session_EXPORT static void leave_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::LeaveGameSession& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Kick member from a game session, only leader can kick member.
    * @details Kick member from a game session, only leader can kick member.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/members/{memberId}/kick
    *
    */
    AB_session_EXPORT static void kick_game_session_member(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::KickGameSessionMember& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Reject a game session invitation.
    * @details Reject a game session invitation.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/reject
    *
    */
    AB_session_EXPORT static void reject_game_session_invitation(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::RejectGameSessionInvitation& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Get Server Secret.
    * @details 
			Used by game Client to Get Secret  
			 constraints
				- EnableSecret = true
				- Type = &#34;DS&#34;
				- secret value will only be produced if enableSecret is true and type is DS

  			 if enableSecret = false
				- empty secret will be returned as 200 OK

			Expected caller of this API
				- Game Client to get server secret

			In the Response you will get following:
				- 200 OK { &#34;secret&#34;: &lt;string&gt; } 

				If there is error: 
				- 400 Invalid path parameters
				- 401 unauthorized
				- 404 StatusNotFound
				- 500 Internal server error
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/secret
    *
    */
    AB_session_EXPORT static void get_session_server_secret(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GetSessionServerSecret& request, 
        const std::function<void(const model::ServerSecret&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Append new member or team to session.
    * @details Append new member or team to session
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/teams
    *
    */
    AB_session_EXPORT static void append_member_to_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::AppendMemberToGameSession& request, 
        const std::function<void(const model::GameSession&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief cancel a game session invitation.
    * @details cancel a game session invitation.
    * @details /session/v1/public/namespaces/{namespace}/gamesessions/{sessionId}/users/{userId}/cancel
    *
    */
    AB_session_EXPORT static void cancel_game_session(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::CancelGameSession& request, 
        const std::function<void()>& on_204,
        const std::function<void (const accelbyte::Error&)>& on_error);   

    /**
    * @brief Query user's game sessions
    * @details Query user&#39;s game sessions.
By default, API will return a list of user&#39;s active game sessions (INVITED,JOINED,CONNECTED).

Session service has several DSInformation status to track DS request to DS providers:
- NEED_TO_REQUEST: number of active players hasn&#39;t reached session&#39;s minPlayers therefore DS has not yet requested.
- REQUESTED: DS is being requested to DSMC.
- PREPARING: DS needs to call manual set ready for the game session
- AVAILABLE: DS is ready to use. The DSMC status for this DS is either READY/BUSY.
- FAILED_TO_REQUEST: DSMC fails to spin up a DS for session.
- DS_ERROR: DS provider fails to spin up the DS or the DS itself becomes unreachable
- DS_CANCELLED: when DSMC is preparing the DS, DSMC will give a temporary DS. In this phase, if you delete the game session, the DS request will be canceled.
- ENDED: when a game session (match) has finished and DS has done its job, it will terminate itself.
- UNKNOWN: if any unknown DS status is detected.
DSInformation has 2 fields for DS status: &#34;status&#34; and &#34;statusV2&#34;. The &#34;status&#34; is there for backward-compatibility, therefore we encourage to just rely on &#34;statusV2&#34; for the more updated statuses.
DS Source can be DSMC, AMS or custom. In DSMC, a DS request will be put in queue if they dont have available buffers, and DSMC will let the service knows when they finished spinning it up. While AMS doesn&#39;t have a concept of queue. Therefore some &#34;DSInformation.statusV2&#34; only applicable for DSMC.

    * @details /session/v1/public/namespaces/{namespace}/users/me/gamesessions
    *
    */
    AB_session_EXPORT static void get_my_game_sessions(
        const accelbyte::tls::SecurityAuthorization& authorization,
        const game_session::GetMyGameSessions& request, 
        const std::function<void(const model::GameSessions&)>& on_200,
        const std::function<void (const accelbyte::Error&)>& on_error);   

};

}
}